# Feature 078 — Panel Tabs: running log

Kept up to date by every task in `tasks.md`. A ticked task there means
"verified", and the evidence is here.

## Status checklist per task

| Task | Status | Note |
|---|---|---|
| T001 | done | this log |
| T002-T008 | done | scaffolding, ids 2860-2880 / 5956-5957 / IDC_TABSTRIP 958 / IDH_TABSTRIP 143 / IDC_PANELTABS 6235 / IDS 14212-14223, strings, Appearance checkbox (`ta&bs`), translator words |
| T009-T011 | done | `src/common/saltabs.*` + `TestPanelTabs078()`: saltests 1353 -> 1405 checks, 0 failed |
| T012-T018 | done | `CPathHistory::IsLocked`, `Configuration.PanelTabs` ("Panel Tabs", default on), `CPanelTab`/`CPanelTabs`, `CFilesWindow` members, `CaptureActiveTab`, `SwitchToTab`, histories over all tabs |
| T019-T020 | done | `CTabWindow` (paint, hit test, hot tracking, click/plus/close/list/middle/drag/context), `ToggleTabStrip`, `SetTabsEnabled`, panel + main layout, `GetDirectoryLineHeight` = strip + line, start-up wiring |
| T021-T026 | done | tab operations, strip interactions, title follows `DirectoryLineSetText`, list button, activation/colour/font/lock hooks, Appearance page + FR-005 confirmation + post-OK relayout |
| T027 | done | US1 driven checks (below): +, retitle, cursor/selection restore, archive title, middle close of the middle tab, inactive-panel tab click, F5 target, re-read on activation, on/off round trip with the confirmation |
| T028-T030 | done | hit test + `IDH_TABSTRIP` mapping; off-state audit (below); leave-semantics trace (below) |
| T031 | partly | matrix (b) driven except move/delete/rename, hot paths, Working Directories, two Drive Bars, bug report, plugin load sweep; SFTP and archive scenarios driven (below) |
| T032-T033 | done | writer/reader; 20-tab save + restore, fresh registry, `Panel Tabs = 0` verified (below) |
| T034 | partly | disk tabs across restarts, fresh/0.1.7 registry, option-off write verified; archive/SFTP tab across a restart, `.reg` export, kill-and-restart, Process Monitor still owed |
| T035-T037 | done | 21 commands, Tabs submenus, four shortcuts + `IsSalHotKey` reservations; driven (below) |
| T038 | partly | skill levels, hotkey refusal, shortcuts, Ctrl+Tab/Tab driven; Ctrl+Shift+T/W with the command line focused and Ctrl+PgUp/PgDn not driven |
| T039-T042 | done | middle click on items and tabs, tooltips, drag reorder, context menu; driven (below) |
| T044 | done | 12 gaps x 8 languages via DeepL (1,664 + 67 chars, quota left 473,088), pins under `_feature_078`, `slt --verify` byte-exact |
| T045 | done | `windows_tabstrip.htm` (new), `windows_panel.htm`, `shortcuts_keyboard.htm` (4 rows), `configuration_appea.htm`, `.hhc`/`.hhk`/`.hhp` |
| T046 | partly | 20 tabs + list button, narrow strip widths, encoding fixtures driven; narrow panel, five-minute idle, 10,000-entry timing, themes, scaling owed |
| T047 | done | `build.cmd full` Debug (189 language modules), the *Left > Tabs* submenu screenshotted in all 8 languages (below) |
| T048 | done | re-run after the last edits: strict guard 0, draft 149 (pre-existing hit only), `src/plugins/shared` diff = the mandated version bump in `spl_vers.h` |
| T050 | partly | `build.cmd full release` exit 0 (Release x64, 20 plugins, 189 language modules, runtime closure OK) + Release smoke (below); the full quickstart pass on that build is the release-gate human step |
| T049 | done | `CHANGELOG.md` 0.1.8 entry, `spl_vers.h` 0.1.8 / build 192 (+ table row), `tandemcommander.iss`, `CLAUDE.md` version line + Recent Changes |
| T043 | partly | middle click on disk/SFTP folders and on tabs, drag, tooltips, tab context menu driven; middle click inside an archive, Escape during a drag and the strip-background menu not driven |

## Decisions taken while implementing

- **T017 verification (research R4)**: `CPathHistory::AddPath` (`src/salamdr3.cpp:1988`) returns early when the new path equals the "actual" item (top of the list, or the item before `ForwardIndex`), and `ChangeActualPathData` (`:1942`) updates only when the path matches the actual item. So swapping `PathHistory` to the target tab **before** `ChangeDir` is correct as designed: the leaving panel's cursor was recorded by the explicit `RefreshPathHistoryData()` call, `ChangeDir`'s own call is a no-op on the target history, and `DirectoryLineSetText` does not duplicate the target's top entry. No `RemoveCurrentPathFromHistory` needed.
- `ToggleTabStrip` lives in `src/paneltabs.cpp` next to the other tab code, not in `fileswn2.cpp` (tasks T020 said "modelled on `ToggleDirectoryLine`" - it is, byte for byte in shape); `fileswn*.cpp` gain only hook lines.
- The strip repaints itself whole on hot-track changes through the shared `ItemBitmap` cache (one `BitBlt`, no flicker) instead of item-wise `GetDC` painting; simpler and the strip is small.
- Hovered *background* tabs use `CurrentColors[HOT_PANEL]` (their fill is `COLOR_BTNFACE`); hovered *active* tabs use `HOT_ACTIVE`/`HOT_INACTIVE` (caption fill) - the palette's intent, contract §3 refined.
- `SalTabTitleFromLocation`: a UNC root is returned whole for plugin file systems too (`nethood:\\server` -> `\\server`) - found by the unit test.
- The first tab owns the panel's `CPathHistory` from the constructor on (the panel pointer always refers to the active tab's history); the destructor no longer deletes it.
- Tooltips answer through `CopyToolTipAnswer` with `LoadStrU8`; the FR-005 confirmation is composed with `LoadStrU8` + `_snprintf_s` and shown with `SalMessageBox` (forwards to `SalMessageBoxEx`, UTF-8-aware as in cmdshell.cpp).

## Verification results

- 2026-09-18 Phase 2 checkpoint: `build.cmd` exit 0 (Debug x64, encoding guard inside), `saltests.exe`: 1405 checks, 0 failed. Screenshot of the running Debug build: each panel shows a one-tab strip (`Downloads` / `Release_x64`) with the `+` button above the Directory Line; Middle Toolbar and panel content laid out below it.

- 2026-09-18 driven GUI checks on the Debug build (PowerShell driver `drive.ps1`: mouse/keys/screenshots against the started process only; UI switched to English for the session by setting `Configuration\Language` before start and restoring `czech.slg` right after):
  - US1 §4: `+` opens a copy tab and activates it; Backspace in it retitles the tab (`Downloads` -> `D:\`); clicking tab 1 shows `D:\Downloads` again with tab 2 still titled `D:\` (after fixing the active-index ordering, see Deviations); returning to a tab restored the cursor (Down+Enter in `D:\` landed on the remembered `Downloads` -> `Eva`); drag of tab 2 before tab 1 reordered the strip; middle click on a tab closed it; right click opened the context menu with *New Tab / Duplicate Tab / Close Tab / Close Other Tabs / Close Tabs to the Right*, the last three greyed on a single tab.
  - US4 §8: Ctrl+Shift+T x3 -> 4 tabs, 4th active; Ctrl+Shift+PgUp -> 3rd; Ctrl+Shift+PgDn twice wrapped to the 1st; Ctrl+Shift+W closed it and the neighbour became active; *Left > Tabs* submenu lists all seven commands with their shortcuts; Ctrl+Tab still focuses the command line.
  - US5 §9: middle click on the `devilutionx-windows-x86_64` folder opened it in a background tab (title with ellipsis), the current tab stayed active.
  - Overflow (FR-009): 20 tabs in the right panel -> one row of shrunken tabs, the active (last) one visible, the `+` and the tab-list button at the right end; the list popup showed all 20 with the current one checked.
  - US3 §7: normal exit (Alt+F4) wrote `Right Panel\Tabs\0..19` (Path/View Type/Sort Type/Reverse Sort/Enable Filter/Filter) and `Active Tab = 0x11`, `Left Panel\Tabs\0` only, the legacy `Path` still the disk path; restart restored the 20 tabs with the same active tab and Ctrl+Shift+PgDn opened the next (lazily restored) one.
  - FR-003: `Panel Tabs = 0` in the registry -> no strip, the Directory Line directly under the Drive Bar, *Left* menu ends at *Refresh* (no Tabs submenu); `Panel Tabs = 1` -> strips and submenu back.
  - Configuration: *Options > Configuration > Appearance* shows *Show tabs in panels* (first build overlapped the Panel Font group - the three groups below were moved down by 12 dialog units); OK without a change leaves the tabs untouched.

- 2026-09-18 T029 off-state audit: with `Configuration.PanelTabs == FALSE` the strip window does not exist (`ToggleTabStrip` destroys it, `SetTabsEnabled(FALSE)` removes every background record so the set holds one), the panel `WM_SIZE` adds a strip height only while `TabStrip->HWindow != NULL`, `GetTabStripHeight()` is 0 and `GetDirectoryLineHeight()` is today's value; every `CFilesWindow` tab operation, `HandleTabCommand`, `OpenIndexInNewTab`, `OnMButtonUp` and the `OnSysKeyDown` block test `Configuration.PanelTabs` first, and the Tabs submenus are removed in `WM_USER_INITMENUPOPUP`. Driven: registry without `Panel Tabs`, `Tabs` and `Active Tab` (the 0.1.7 shape) starts with one-tab strips; exit wrote `Panel Tabs = 1`, `Right Panel\Tabs\0`, `Active Tab = 0`. `Panel Tabs = 0` starts without strips and without the submenu (earlier run).
- 2026-09-18 T030 leave-semantics trace: `SwitchToTab` calls the unchanged `ChangeDir` (no diff in `fileswn2.cpp`), so the archive branch of `PrepareCloseCurrentPath` (update offer) and the plugin branch (`TryCloseOrDetach`) run exactly as for any path change; `CHPPFR_CANNOTCLOSEPATH` takes the revert path (active index, sort, filter, view, history pointer restored, `DirectoryLineSetText`, deferred refresh). `CloseTab` on a background tab is `CPanelTabs::Remove` -> `delete CPanelTab` -> `delete CPathHistory`: `CPathHistoryItem` owns only strings and an `HICON`; its `PluginFS` pointer is not owned (no release call), so no plugin and no `DetachedFSList` entry is touched; `ClearPluginFSFromHistory` already runs over every tab's history (T018), so a closed FS never leaves a dangling pointer in a background tab.
- 2026-09-18 driven checks, second series (Debug build, English UI, driver `drive.ps1` now types into a modal dialog of the started process instead of forcing the main window to the foreground):
  - §4.3 archive title: Enter on `MSIAfterburnerSetup.zip` -> tab `MSIAfterburnerSetup....` (ellipsis of the archive name), Directory Line `D:\Downloads\MSIAfterburnerSetup.zip`.
  - §4.4: three tabs, middle active, Ctrl+Shift+W -> the right neighbour became active (2 tabs left, second highlighted).
  - §4.5: left panel active, click on the right panel's first (inactive) tab -> right panel active, that tab shown, caret on `..` in the list, command line prompt switched to the right path.
  - §4.6: left tab on the fixtures directory, F5 on `Nový projekt` -> the Copy dialog offered `E:\Projects\tandemcommander\build\tandemcommander\*.*` (the right panel's active tab), the copy landed there.
  - §4.7: F7 `tc078dir` in right tab 1, click tab 2 (same directory) -> `tc078dir` listed (re-read on activation).
  - §9.4 tooltips: `+` -> "New Tab (Ctrl+Shift+T)"; a tab -> its full location `D:\Downloads\MSIAfterburnerSetup.zip` (the tooltip needs a mouse move inside the strip, as every Salamander tooltip does).
  - §12 encoding fixtures (`build\tc078fixtures`): titles `Lone<notdef>surrogate`, `Nový projekt`, `中文文件夹` match the Directory Line glyph for glyph (the main window caption shows `?` for both - the ANSI caption is pre-existing and out of scope).
- 2026-09-18 T048 hygiene, first pass: `check_encoding.py --strict` TOTAL 0; `--draft` TOTAL 149, the only hit in a touched file (`fileswn0.cpp:347`, `MultiByteToWideChar(CP_ACP...)` for the OLE name) is present at `2c0cba9` (pre-078); `git diff --stat -- src/plugins/shared` empty (interface 106); all six new files UTF-8 BOM; `clang-format` 17.0.3 (VS 2022 LLVM) applied to `saltabs.*`, `paneltabs.*`, `tabwnd.*`.
- **Open observation - one leak report**: the first exit of this series (registry-restored 20 tabs, Appearance page tabs off -> Yes -> on, Alt+F4) ended in the Debug heap checker's "Detected memory leaks!" box. The CRT dump goes to the debugger stream, which nothing was capturing. Under a DBWIN listener (`dbglisten.ps1`) the same flow with 3 tabs, the 20-tab save + restore + exit, the fresh-registry start and a plain start/exit all exited clean (no dump, no box). Not reproduced in 5 runs; kept open here and in the closing report, the listener stays armed for every further driven run.

- 2026-09-18 driven checks, third series (matrix (b), plugin file systems, archives):
  - §5(b): Ctrl+U swapped the panels **with their tab sets** (2 tabs moved left, 1 moved right) and back; Ctrl+F11 zoomed the left panel with its strip intact and restored; Ctrl+F10 opened Compare Directories on the two active tabs; Alt+Left/Right walked the active tab's own history (title followed); the command line accepted input (`cd ..` shows the upstream "use Shift+F7" note - unchanged behaviour); Files, Commands, Plugins and Right menus open normally, Right ends with *Tabs*; the *Tabs* submenu is present at Beginner, Intermediate and Advanced skill level; Plugins Manager > Keyboard: Ctrl+Shift+T is refused ("This shortcuts belongs to Tandem Commander. Please, choose another one.", Assign disabled).
  - §6 SFTP (`tctest@127.0.0.1:2222`, container `tandem-sftp`): connect in the left tab -> title `tctest@127.0.0.1:2222` (root rule); Ctrl+Shift+T -> second tab on the same session, no login; `home` in tab 2 -> title `home`; back to tab 1 -> root listing, no prompt; middle click on `etc` -> background tab `etc`; Ctrl+Shift+W on the root tab -> `home` shown; Ctrl+Shift+PgDn -> `etc`; Shift+F7 to a disk path leaves the FS as any navigation does; closing the last SFTP tab likewise. The SFTP plugin's `TryCloseOrDetach` (v1) answers `detach = FALSE`, so every leave closes the session and every return reconnects from the saved bookmark without a prompt - that *is* the plugin's rule (Clarification Q1); the numbered rows at the bottom of Alt+F1 are hot paths, not kept connections.
  - §6 archive (`build\tc078fixtures\test.zip`, file edited via F4 and modified on disk): clicking another tab shows the "archive is about to close" note, then *Archive Update* listing `soubor.txt`; its close box asks "ignore all updated files?" and *No* returns to the dialog; *Ignore All* -> *Yes* leaves, the target tab (fixtures) is active and intact, the archive tab stays in the strip and re-enters on click. The dialog has no "stay" answer: the refusal of a leave (`CHPPFR_CANNOTCLOSEPATH`) comes only from the packer plugin's `CanCloseArchive` question or a plugin FS answering FALSE, neither of which the ZIP/SFTP plugins raise here, so the revert path of `SwitchToTab` stays verified by code reading only (see Deviations).
  - Same archive flow with tabs **off** (`Panel Tabs = 0`, Shift+F7 in and out): identical dialogs, clean exit.
- **Incident, once**: in the first archive run the leave ended with the panel in `D:\Downloads` and the *second* tab active and retitled `Downloads` instead of the fixtures tab. Three exact re-runs (same answers, including the X -> No detour, and one with a `subst` drive appearing while the prompt was open) ended correctly. The one environmental difference of the first run: a USB stick (drive `H:`) was plugged in while the leave prompts were open, and a device arrival makes the main window refresh the panels - a re-entrant refresh while the tab bookkeeping is half switched. Mitigation applied: `SwitchToTab` brackets `ChangeDir` with `BeginStopRefresh()`/`EndStopRefresh()`, exactly what `ChangeDir` itself does around the Change Directory dialog, so snooper and plugin refreshes are deferred until the switch is complete. Kept as an open item in the closing report (not reproduced with the guard either way).
- **Leak report, dump captured**: the DBWIN listener caught the second occurrence (pid 27840, the archive run): one block, `normal block at ..., 88 bytes long`, first 16 bytes zero, `#File Error#(84)` - the CRT could not read the source file name, which happens for blocks allocated by a module unloaded before the dump, i.e. a plugin (only ftp, pictview and unchm register with `AddModuleWithPossibleMemoryLeaks`). No product source allocates 88 bytes on a line 84 of a core file. Clean exits: plain start, 3-tab option toggle, 20-tab save + restore, fresh registry, the long US1-US5 session (SFTP + archive + menus), the tabs-off archive run. Open item with the listener recipe (`dbglisten.ps1`); identifying the block needs a debugger with `_CrtSetBreakAlloc` or the plugins registering their module names.
- 2026-09-18 T044: `build_langs.cmd --export-templates --module salamand` (1/1), `translate.merge --module salamand` dry run 12 unique gaps per language (~280 chars), real run 1,664 DeepL characters, one validation failure (Hungarian doubled `%d`, kept English) and four wrong machine results found by reading: Spanish *Next Tab* rendered as "Página siguiente", the Spanish and Romanian confirmations garbled ("[p]", "fereastra"), tooltips with localised key names ("Strg+Umschalt+T", "Ctrl+Maj+T", "Ctrl+Mayús+T") while the menu rows keep the English labels, Czech checkbox "záložky" vs the menu's "karty", Hungarian checkbox "Fülök" vs "lap". Pins under `salamand/_feature_078` in `ui-overrides.json` (German switched to *Registerkarte* throughout per research R20; cs/sk/hu/ro/fr/es confirmations rewritten with one `%d`), second merge 67 characters, `slt --verify` byte-exact for all 298 files. Quota left 473,088.
- 2026-09-18 T045: manual pages written (`windows_tabstrip.htm` new with the 2026 footer; rows/option/TOC/index/alias edits keep the surrounding pages' encodings and CRLF).
- 2026-09-18 T049: version 0.1.8 / build 192 in `spl_vers.h` (table row added), `tandemcommander.iss`, `CLAUDE.md`; `CHANGELOG.md` entry with *Added* (panel tabs, manual) and *Changed* (Ctrl+Shift+Page Up/Down).

- 2026-09-18 T047: after `build.cmd full` (Debug, 189 language modules) the Debug build was started once per language (`Configuration\Language` set before each start) and *Left > Tabs* opened: cs *Karty* (Nová karta, Duplikovat kartu, Zavřít kartu, Další/Předchozí karta, Zavřít ostatní karty, Zavřít karty vpravo), de *Registerkarten* (pinned wording), fr *Onglets*, nl *Tabbladen*, hu *Lapok*, ro *File*, sk *Karty*, es *Pestañas*; the shortcut labels stay `Ctrl+Shift+...` everywhere; greying matches the single-tab state. The Spanish panel drew `ñ` as `n` (also in the pre-existing *Tamaño* header): the ANSI header/menu drawing under the Czech system code page, cluster B-1 of feature 069, not this feature.
- 2026-09-18 Release: `build.cmd full release` -> exit 0, 0 min 40 s incremental, `Release_x64\tandemcommander.exe` 3,358,720 bytes, 20 plugins in `plugins.ver`, 9 `.slg`, 4 runtime DLLs shipped with closure OK. Smoke on the Release binary (English UI): strips present, Ctrl+Shift+T + Backspace gave `tc078fixtures | build ×`, clicking tab 1 restored the fixtures listing with tab 2 intact, Ctrl+Shift+W closed the active tab and the neighbour took over, Alt+F4 exited cleanly.
- **Leak report, pattern found**: all three "Detected memory leaks!" exits were the *first start after a rebuilt executable* (builds 11, 12 and 13-full), always the same 88-byte block with an unreadable module name. Later starts of the same binaries - eleven runs including the eight language starts - were clean, and neither touching one plugin (`peviewer.spl`) nor touching all twenty plugin files before a start reproduced it, so the trigger is tied to the new executable's first run (whatever Salamander re-reads then), not to tabs and not to the plugin file times. Recorded as an open item; it needs a debugger on that first start.
- Cleanup: `build\tc078fixtures` removed (PowerShell `Remove-Item -LiteralPath`, the lone-surrogate name defeats the POSIX tools), the F4 editor instances closed, `Configuration\Language` back to `czech.slg`, `Panel Tabs` back to 1. The Debug and Release runs shared the user's registry; the user's own 0.1.7 instance was left running throughout and will rewrite its own values on exit.

## Deviations from the plan

- `SwitchToTab` first set `Tabs.ActiveIndex` only after `ChangeDir`; the title hook in `DirectoryLineSetText` (T023) then wrote the new location into the *old* tab's record. Fixed by making the target active before `ChangeDir` and restoring the old index on a refused leave (found by the first driven check, u2).
- The first driven check picked the largest window of *any* `tandemcommander.exe` and so sent a Backspace to the user's own running instance and force-stopped it at the end; the driver now addresses only the process it started (by pid, topmost while driving) and the user's instance was restarted.
- quickstart.md §6.2 assumed the archive-update prompt offers *Cancel*; the *Archive Update* dialog only updates or ignores, so the "refusal keeps the tab" scenario is driven for plugins that ask (none of the shipped ones do) and verified in code for `CHPPFR_CANNOTCLOSEPATH` otherwise.
- While the leave prompts of a switch are open, the strip already paints the *target* tab as active (the active index is set before `ChangeDir` so the title hook writes to the right record); a refused leave repaints the old one. Cosmetic; noted for the closing report.
- The first wiring script was re-run after a failed anchor and doubled the first group of edits (its idempotency check was wrong); the affected files were reverted with `git checkout` and re-applied once - no trace in the tree, recorded for honesty.
